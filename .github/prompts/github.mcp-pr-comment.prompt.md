---
agent: github.mcp-pr-comment
---

## User input

```text
$ARGUMENTS
```

You **must** use non-empty **`$ARGUMENTS`** (trimmed) as the PR comment **body** when the user supplied text after invoking this prompt. If **`$ARGUMENTS`** is empty, follow the fallback order in the agent profile for **body** (Additional instructions, delegation, etc.).
