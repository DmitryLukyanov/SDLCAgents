{
  "name": "stub_agent_example",
  "description": "Example config-first agent with an intentionally unimplemented runner (validation demo).",
  "model": "o4-mini",
  "params": {
    "runner": "pipeline",
    "steps": [
      {
        "id": "only_step",
        "runner": "this_runner_is_not_implemented_anywhere",
        "enabled": true
      }
    ]
  }
}
