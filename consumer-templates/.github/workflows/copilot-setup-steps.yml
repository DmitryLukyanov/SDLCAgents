# Copilot Coding Agent environment setup.
# This workflow configures the environment that Copilot uses when
# working on issues assigned to it. It runs before Copilot starts coding.
#
# Customize this file to install dependencies your project needs
# (e.g., npm ci, pip install, etc.).
name: Copilot Setup Steps

on: workflow_dispatch

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Add your project-specific setup here, for example:
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      # - run: npm ci
