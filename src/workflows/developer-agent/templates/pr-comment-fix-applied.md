<!-- speckit-agent: fix -->
## Spec-Kit: Fix Applied

Targeted fixes committed to `{{BRANCH_NAME}}`.

_The spec gate will re-validate the artifacts. If clean, the pipeline continues automatically._
_If issues remain: use `/fix <instructions>` again, or `/proceed` after manual edits._
{{RUN_LINK}}