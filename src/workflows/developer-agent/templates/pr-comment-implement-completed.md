<!-- speckit-agent: implement -->
## Spec-Kit: {{STEP_LABEL}} complete

The `implement` step has completed. The PR is ready for human review.

_If your pipeline includes **code_review**, it runs next after the spec gate passes; then check `code-review-summary.md` before merging._
{{RUN_LINK}}