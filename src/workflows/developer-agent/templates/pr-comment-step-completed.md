<!-- speckit-agent: {{STEP}} -->
## Spec-Kit: {{STEP_LABEL}} complete

The `{{STEP}}` step has been completed. Artifacts committed to `{{BRANCH_NAME}}`.

_The spec gate will validate the artifacts. If clean, `{{NEXT_STEP}}` starts automatically._
_If issues are found: use `/fix <instructions>` for targeted corrections, or `/proceed` after manual edits._
{{RUN_LINK}}